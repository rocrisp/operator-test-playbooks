apiVersion: "operators.coreos.com/v1alpha1"
kind: "CatalogSource"
metadata:
  name: "test-operators"
  namespace: "openshift-marketplace"
spec:
  sourceType: grpc
  endpoint: "https://quay.io/cnr"
  image: quay.io/user/{{ quay_namespace }}/{{ openshift_namespace }}:v4.7
  displayName: "Test Operators"
  publisher: "CVP"
  authorizationToken:
    secretName: marketplacesecret
