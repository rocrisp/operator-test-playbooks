apiVersion: "operators.coreos.com/v1alpha1"
kind: "CatalogSource"
metadata:
  name: "test-operators"
  namespace: "openshift-marketplace"
spec:
  sourceType: grpc
  endpoint: "https://quay.io/cnr"
  image: {{ quay_namespace }}
  displayName: "Test Operators"
  publisher: "CVP"
  authorizationToken:
    secretName: marketplacesecret
